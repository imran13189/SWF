@using SWF.Models
@model UsersModel
@{
    ViewBag.Title = "ContactUs";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="inner_pages_bx contact_bg">
    <div class="container">

        <div class="listing_table">

            <h2 class="project-title">Contact us Form</h2>

            @using (Html.BeginForm("Contact", "UserProfile", FormMethod.Post, htmlAttributes: new { id = "", enctype = "multipart/form-data" }))
            {
                 <div class="Error_Message">@ViewData["error"]</div>
                <div class="full-row">
                        
                    <div class="col-lg-6 no_margin">
                        <div class="full-row">
                            <h4 class="translate_head">First Name</h4>

                            @Html.TextBoxFor(m => m.FirstName)<br />
                            @Html.ValidationMessageFor(m => m.FirstName)
                        </div>

                        

@*                        <div class="full-row">
                            <h4 class="translate_head">UserName</h4>
                            @Html.TextBoxFor(m => m.Username)
                            @Html.ValidationMessageFor(m => m.Username)

                        </div>*@

                        

                        <div class="full-row">
                            <h4 class="translate_head">Phone Number</h4>
                            @Html.TextBoxFor(m => m.PhoneNumber)
                            @Html.ValidationMessageFor(m => m.PhoneNumber)

                        </div>

                        
                          <div class="full-row">
                            <h4 class="translate_head">Query</h4>
                            @Html.TextAreaFor(m => m.Query,new { style="width:320px;" })
                            @Html.ValidationMessageFor(m => m.Query)

                        </div>

                    </div>

                    <div class="col-lg-6 no_margin">
                    <div class="full-row">

                            <h4 class="translate_head">Last Name</h4>

                            @Html.TextBoxFor(m => m.LastName)
                            @Html.ValidationMessageFor(m => m.LastName)

                        </div>
                   @* <div class="full-row">
                            <h4 class="translate_head">Password</h4>
                            @Html.PasswordFor(m => m.Password)
                            @Html.ValidationMessageFor(m => m.Password)

                        </div>*@
                       
                    <div class="full-row">
                            <h4 class="translate_head">Address</h4>
                            @Html.TextBoxFor(m => m.Address)
                            @Html.ValidationMessageFor(m => m.Address)

                        </div>
                    @*<div class="full-row">
                    </div>*@
                        <div class="full-row top_mrgn">
                        <input type="submit" value="Send Query" style="margin:30px;" name="Update" class="btn btn-primary btn-create" />
                        <a class="btn btn-primary btn-create" style="margin:30px;" href="@Url.Action("Index", "Dashboard")">Cancel</a>
                    </div>
                        </div>
                    


                </div>
            }

        </div>
    </div>
</div>


